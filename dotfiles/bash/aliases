# processes
alias tu='top -o %CPU' #cpu
alias tm='top -o %MEM' #memory
alias tt='open -a TextEdit' #editor

# fs
alias ..='cd ..'
alias cdd='cd -'

function take {
  mkdir $1
  cd $1
}

# git
alias g='git status'

# sdwan
alias sdwan_deploy="cd ~/WebstormProjects/sdwan_gui && npm run build && mv build sd-wan_gui_s2s && scp -r ./sd-wan_gui_s2s sdwan_dev:~/ && rm -rf ./sd-wan_gui_s2s"
alias sdwan_vpn="sudo openvpn —config /etc/openvpn/edub.ovpn"


# bash
alias reload_bash="source ~/.bash_profile"
